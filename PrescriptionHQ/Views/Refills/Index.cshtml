@model IEnumerable<PrescriptionHQ.Models.Refill>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RequestDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PickupStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RequestDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PickupStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RefillId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RefillId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RefillId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
